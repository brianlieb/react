In this lab we will create a DisplayAirportStatus that will display either only airports with delay or only airports with no delay.

Commands you need:
npm install
npm test

To view the coverage:
npm run coverage

To lint:
npm run eslint

After all tests pass:
npm run build
npm start